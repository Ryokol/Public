#include<stdio.h>
#define DIM 5

int main()
{
    int a[]={4,3,8,5,1};
    int reovato = 0,i,elemento;
    scanf("%d",&elemento);
    i=0;
    while (!trovato && i<DIM)
    {
        if(a[]...)
        {
            ...
        }
    }

}
//trovare un numero dentro al array
