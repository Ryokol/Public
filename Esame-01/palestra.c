#include<stdio.h>
#include<string.h>

typedef struct canzone
{
    char *parola[10];
    int *riga;
        struct *next;
        
}nodo;

int main()
{
    FILE *file;
    int i=0;
    char *pointer;
    pointer = &nodo;

    file=fonep("file-o1.txt","r");
    if(f==NULL)
    {
        printf("Lettura file errata");
    }
    while(pointer!=NULL)
    {

        i++
    }
}